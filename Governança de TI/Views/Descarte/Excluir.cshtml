@model Governança_de_TI.Models.DescarteModel

@{
    ViewData["Title"] = "Excluir Registo de Descarte";
}

<div class="row justify-content-center">
    <div class="col-lg-8 col-xl-6">
        <div class="card shadow-sm">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">Confirmar Exclusão</h4>
            </div>
            <div class="card-body p-4">
                <p class="lead">Tem a certeza de que deseja excluir permanentemente o seguinte registo de descarte?</p>

                <div>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4">ID do Descarte</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Id)</dd>

                        <dt class="col-sm-4">Equipamento</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Equipamento.Descricao)</dd>

                        <dt class="col-sm-4">Empresa Coletora</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.EmpresaColetora)</dd>
                    </dl>

                    <form asp-action="Excluir">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-end mt-4">
                            <a asp-action="Consulta" class="btn btn-outline-secondary me-2">Cancelar</a>
                            <input type="submit" value="Confirmar Exclusão" class="btn btn-danger" />
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
</div>

